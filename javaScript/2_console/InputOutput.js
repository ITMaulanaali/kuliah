let nama;
let nim;

nama = prompt("Masukkan nama");
nim = prompt("Masukkan Nim");

console.log(`Anda adalah: ${nama} dengan nim: ${nim}`)

/**
 * Untuk menyisipkan data ke dalam string harus didalam template literal(ini berbeda dengan string). Untuk membuat template literal menggunakan backtiks`
 * ada juga alert() dan confirm()
 */